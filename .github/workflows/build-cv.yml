name: Build CV

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: build-cv-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install poppler-utils
        run: sudo apt-get update && sudo apt-get install -y poppler-utils

      - uses: TeX-Live/setup-texlive-action@v3
        with:
          version: 2025
          packages: >-
            scheme-basic
            latexmk
            preprint
            tools
            hyperref
            glyphtounicode
            babel-english
            xcolor
            enumitem
            titlesec
            fancyhdr
            marvosym
            ragged2e
            footmisc
            fontawesome
            collection-fontsrecommended

      - name: Build PDF
        # checkout resets mtimes; touch source so make always runs latexmk
        run: touch cv_en.tex && make cv

      - name: Verify single-page PDF
        run: test "$(pdfinfo CV.pdf | awk '/^Pages:/ {print $2}')" -eq 1

      - name: Verify committed PDF matches build
        if: github.event_name == 'pull_request'
        run: git diff --exit-code CV.pdf

      - name: Commit updated PDF
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "ci: rebuild CV.pdf from cv_en.tex [skip ci]"
          file_pattern: CV.pdf
